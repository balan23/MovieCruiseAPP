spring:
 jpa:
  properties:
   hibernate:
    dialect: org.hibernate.dialect.MySQL5Dialect
  hibernate:
   ddl-auto: none
 datasource:
  url: ${MYSQL_CI_URL}
  username: ${MYSQL_USER}
  password: ${MYSQL_PASSWORD}
 